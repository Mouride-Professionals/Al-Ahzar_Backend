{
	# Optional: override via env var in docker-compose.yml
	email {$CADDY_EMAIL}
}

# Replace with your real domain, e.g. api.example.com
{$DOMAIN:localhost} {
	encode zstd gzip
	reverse_proxy strapi:1337
}

